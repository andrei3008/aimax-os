[Unit]
Description=AiMax UI (React build served with npm serve)
After=aimax-api.service

[Service]
Type=simple
User=dev
WorkingDirectory=/opt/aimax/ui/aimax-ui
ExecStart=/usr/bin/npx serve -s dist -l 5173
Restart=always

[Install]
WantedBy=multi-user.target
